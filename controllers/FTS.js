'use strict';

var url = require('url');

var FTS = require('./FTSService');

module.exports.extract digest = function extract digest (req, res, next) {
  FTS.extract digest(req.swagger.params, res, next);
};

module.exports.extract digest from generated link example = function extract digest from generated link example (req, res, next) {
  FTS.extract digest from generated link example(req.swagger.params, res, next);
};

module.exports.login = function login (req, res, next) {
  FTS.login(req.swagger.params, res, next);
};

module.exports.receipt JSON = function receipt JSON (req, res, next) {
  FTS.receipt JSON(req.swagger.params, res, next);
};

module.exports.receipt correctness = function receipt correctness (req, res, next) {
  FTS.receipt correctness(req.swagger.params, res, next);
};

module.exports.restore password = function restore password (req, res, next) {
  FTS.restore password(req.swagger.params, res, next);
};

module.exports.sign Up = function sign Up (req, res, next) {
  FTS.sign Up(req.swagger.params, res, next);
};
